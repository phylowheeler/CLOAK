Directory containing scripts and datatables used to generate figures in the paper

In files where variables are grouped by diff1, etc... 
    *diff1 = change in distance with cloak filtering vs. unfiltered
    *diff2 = change in distance with divvier filtering vs. unfiltered
    *diff3 = change in distance with taper filtering vs. unfiltered
    *diff4 = change in distance with divvier partial filtering vs. unfiltered
    *diff5 = change in distance with propagate error vs. unfiltered
